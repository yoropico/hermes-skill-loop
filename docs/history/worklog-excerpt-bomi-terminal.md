# worklog excerpt — bomi-terminal, pre-extraction

Every `[skill-loop]` / `[hermes-bct]` / `[hermes-cleanup]` / `[curator-key-fix]` /
`[curator-observ]` / `[hermes-plugin]` entry from the `bomi-terminal` repo's
`.claude/worklog.md`, verbatim and in order, covering 2026-07-14 to 07-30 — the
period when this loop was built and deployed by the BCT terminal app.

`.claude/worklog.md` in this repo carries the distilled version. This file is the
unabridged source, kept so the reasoning is not one repo away.

- 2026-07-14 | [skill-loop] Skill self-learning loop LANDED onto master: Hermes Curator DESIGN ported to Claude Code hooks (skill_meta/usage/learn/curator/bootstrap in .claude/skill-loop/, artifacts to ~/.claude/, x-origin marker firewall so BCT skills untouched). BCT deploy via SkillLoopDeploy.swift (bundle folder-ref -> ~/.claude/scripts/skill-loop/ + bootstrap on launch) + StatusbarBridge global-idle curator trigger. 27 pytest + 3 swift-test + full BCT suite green; live smoke (isolated HOME, sys py3.9) confirms 3.9-compat + idempotent bootstrap. Rebased clean onto origin/master (excluded prior unrelated chat base add038f from fix/chat-roster-bind-label) -> single squashed commit.
- 2026-07-14 | [skill-loop][deploy] Deployed to /Applications/BCT.app: Release arm64 (BUILD SUCCEEDED, payload bundled at Contents/Resources/scripts, arm64-only), ditto, Apple-Dev re-sign (K83K59TGLX / TeamIdentifier G7J2LY4LP9, NOT adhoc), codesign --verify OK. LIVE-VERIFIED activation: open -g -n fresh instance -> SkillLoopDeploy populated ~/.claude/scripts/skill-loop/ in 2s + merged SessionEnd(learn)/PreToolUse(usage) hooks into settings.json + seeded skill-loop.json. Skill self-learning loop now ACTIVE for every Claude Code session.
- 2026-07-14 | [skill-loop][bugfix] Found + fixed a LIVE infinite-recursion defect the landing review missed. SessionEnd->learn.py shells out to `claude -p`, and `claude -p` ITSELF fires SessionEnd (confirmed authoritatively via claude-code-guide/docs; headless end reason=prompt_input_exit) -> nested learn.py -> another `claude -p` -> unbounded chain (each up to 300s, transcript grows each level). No reentrancy guard existed anywhere; curator.py has the same exposure (its `claude -p` also triggers SessionEnd->learn). Also found `enabled` was DEAD config on the hook path: only the Swift idle-watcher (SkillLoopDeploy.swift:76) honored it; learn/usage/curator ignored it -> no kill-switch to stop the recursion. Fix (TDD, 6 new tests, 33 pass): env-sentinel SKILL_LOOP_INTERNAL set on every `claude -p` we spawn (learn+curator default_claude) and checked at learn.py main() top -> the nested session's learn no-ops (bounds recursion to depth 1). Rejected the docs' `reason` field as the guard: interactive NORMAL exit is also prompt_input_exit, so gating on it would silently drop the primary learn case. Additionally made `enabled:false` a true kill-switch in learn/usage/curator main(). Redeployed the 3 fixed scripts to ~/.claude/scripts/skill-loop (immediate mitigation for CLI sessions; .prefix-bak rollback copies kept). DURABLE fix still pending the gate: repo commit/PR + Release arm64 rebuild & Apple-Dev re-sign of /Applications/BCT.app bundle (deploy() overwrites unconditionally, so the stale bundle payload would clobber ~/.claude on the next BCT relaunch; in-place bundle patch is banned — breaks the codesign -> IME).
- 2026-07-14 | [skill-loop][bugfix][landed] Recursion-guard fix LANDED: commit 3e1657e -> PR #200 -> master fa778a0. Live-verified end-to-end BEFORE landing (yoros: "verify then land"): guard run (SKILL_LOOP_INTERNAL=1 set on `claude -p`) spawned 0 nested `claude -p --model` distills (env IS inherited by the SessionEnd hook subprocess -> learn.py no-ops); control run (no sentinel) spawned exactly 1 distill then stopped (recursion real + bounded to depth 1). No junk skill produced (trivial transcript -> create:false). Durable deploy complete: repo committed, ~/.claude/scripts/skill-loop patched, /Applications/BCT.app bundle payload patched + Apple-Dev re-signed (codesign -v VALID, flags=none NOT adhoc, TeamIdentifier G7J2LY4LP9 -> IME safe) so a BCT relaunch's deployAsync won't clobber the fix. 33 tests pass. .prefix-bak rollback copies removed.
- 2026-07-14 | [skill-loop][tune] Promoted the loop model sonnet-5 -> claude-opus-4-8 in ~/.claude/skill-loop.json (Next-2, higher-quality distill/curation). Validated the model id first via a sentinel-guarded `SKILL_LOOP_INTERNAL=1 claude -p --model claude-opus-4-8` probe (rc=0, "ok", no distill spawned). Durable: bootstrap seed_config is seed-if-missing (won't clobber the live edit on relaunch); repo config.default.json left at sonnet-5 as the cheaper fresh-install default. No restart needed (load_config read per-invocation; live learn/curator now read opus). NOTE: `model` is shared by learn (fires every session-end) AND curator (24h) -> opus raises per-session distill cost; if that bites, split into learn_model/curator_model (small code change) so frequent distill can stay cheaper while rare curation keeps opus.
- 2026-07-14 | [skill-loop][feat] Split the loop model into per-role overrides (learn_model / curator_model). Rationale: `model` was shared by learn (fires EVERY session end -> frequent) and curator (24h -> rare), forcing one cost/quality tradeoff; opus-for-everything raised per-session distill cost. New precedence in learn_model()/curator_model(): `<role>_model` > shared `model` > default claude-sonnet-5. Live ~/.claude/skill-loop.json now: model=claude-sonnet-5 (learn stays cheap), curator_model=claude-opus-4-8 (rare curation gets quality). repo config.default.json left minimal (model only) as the safe cheap fresh-install default; README documents the override keys + that skill-loop.json edits are durable (seed-if-missing). TDD: 4 new tests (precedence + default_claude uses resolved model), 37 pass. Redeployed learn/curator to ~/.claude; verified deployed+live-config resolution learn=sonnet curator=opus.
- 2026-07-15 | [hermes-bct][audit] Audited the 12 skills the loop actually learned on 7/14. Contract compliance 12/12
- 2026-07-15 | [hermes-bct][learn-loop] Root-caused defect (b) as a DESIGN gap, not a bad distill: learn.md could only
- 2026-07-15 | [hermes-bct][deploy] PR #207 merged (64feba6) and deployed. Payload-only change, so NO rebuild was
- 2026-07-23 | [hermes-cleanup] Closed DRAFT PR #191 (mirror BCT control skills into ~/.hermes) and deleted claude/hermes-integration-6o4e08 (local+remote). Rationale: learned skills absorbed directly via skill-loop, so Hermes-side mirroring is superseded; content remains recoverable in the closed PR.
- 2026-07-30 | [curator-key-fix] Removed the real NousResearch Hermes Agent (v0.19.0, installed 7/14 and updated 7/30 06:57 from another session). Rationale, checked before touching anything: it is a SEPARATE agent whose bundled/learned skills live in ~/.hermes/skills, which Claude Code never reads -- so it could not serve the ~/.claude/skills loop BCT actually feeds, and BCT has zero code references to ~/.hermes (grep over swift/py/yml/json), only doc mentions. `hermes uninstall` hard-gates on sys.stdin.isatty() (main.py _require_tty) so the Bash tool cannot drive it; used the officially supported non-interactive module entrypoint instead (`python -m hermes_cli.uninstall --mode lite`, which pins yes=True and skips the tty gate), run under an EXTERNAL uv-managed python 3.12 rather than the venv's own interpreter (it deletes that venv) and not system 3.9 (hermes_constants uses `str | object`, PEP 604). That external python has no pyyaml, so the uninstaller's gateway-service step aborted with "No module named 'yaml'" -- verified by hand that there was nothing to clean (no user/system launchd plist, no loaded job, no process). Then rm -rf ~/.hermes per yoros; 1.2G reclaimed, no residue in ~/.local/bin, ~/Library, /Applications. Credentials in auth.json/.env went with it (re-login recovers). BCT's skill-loop verified untouched afterwards: 7 payload scripts, 71 skill dirs / 63 learned, both hooks still wired, bundle payload intact.
- 2026-07-30 | [curator-key-fix] Root-caused the skill-loop curator as a LIVE NO-OP since its very first commit (152b8e69, 7/14): prompts/curate.md instructs the model to reply [{"slug":...,"op":...}] but apply_actions read act.get("skill"), so every proposed action resolved to a None slug, fell through the "path does not exist" continue, and was silently discarded. Evidence, not inference: ~/.claude/skills/_archive never existed after 16 days and ~16 curator runs on opus-4-8, 32 of 63 learned skills never used and never triaged, the sole pinned skill predates the code path -- while .curator_state kept stamping last_run so the loop LOOKED healthy. It survived review because test_main_threads_configured_age_floor fed the applier the REAL prompt key ("slug") and then asserted only that the skill SURVIVED, crediting the age floor for what was actually the key mismatch; it passed with min_age_days=0 just as well. Fix accepts BOTH keys (slug primary since it mirrors the manifest field the model sees, skill as synonym) instead of rewriting the prompt, so a model echoing either one lands. The test changes are the real deliverable: the age-floor test is now DIFFERENTIAL (floor 7 vetoes, floor 0 actually archives) so a dropped action can never again masquerade as a veto, plus a contract test that parses curate.md's documented reply shape and asserts the applier honours that key -- editing the prompt alone now fails the suite. 55 pytest (was 53), full BCT gate green (XCTest 902/0, Swift Testing 3217 tests in 403 suites, ** TEST SUCCEEDED **, rc=0), py3.9 import + py_compile OK. Deliberately NOT deployed yet to ~/.claude/scripts/skill-loop or the BCT bundle: activating the fix means the next idle curator run really moves skills into _archive, so the live rollout waits on yoros.
- 2026-07-30 | [curator-key-fix] Recorded the Hermes reference version pin the original spec only ASKED for and nobody ever wrote: upstream a4973c3f / v0.19.0 (2026.7.20), captured off the install right before deleting it. Why it matters: Evolution strategy (A) is the design's ONLY link to Hermes -- diff agent/curator.py on upgrade, fold perspectives into prompts/curate.md -- and a diff needs a baseline. Without the pin that path existed on paper only and was never exercised once in 16 days. Also documented that no local install is needed for it (public repo, fetch on demand), so removing ~/.hermes costs nothing here. Runtime coupling to Hermes remains zero -- grep over .claude/skill-loop/ finds no "hermes" string at all -- so Hermes version-ups genuinely cannot break the loop. The real version-up exposure is elsewhere and shares the no-op's root cause: Claude Code hook-contract drift (SessionEnd payload keys, PreToolUse firing for Skill, async semantics), a retired model id in ~/.claude/skill-loop.json (curator_model is still the older-generation claude-opus-4-8), and a stale BCT bundle re-deploying an old payload over a live patch -- every one of which curate()'s bare `except Exception: return []` would swallow into a silent no-op. That is the argument for making observability the next unit, not a nice-to-have.
- 2026-07-30 | [curator-observ] Made the loop's silent paths visible, because the previous unit proved the failure mode is not "a bug" but "a bug nobody could see": the curator applied nothing for 16 days and NO artifact anywhere could have revealed it -- `except Exception: return []` returned the same empty list for a dead model id, an unparseable reply, a pinned-skill veto, and a genuine "keep everything", while .curator_state stamped last_run either way. New runlog.py appends one JSONL line per run to ~/.claude/skill-loop.jsonl (`log_path` overridable), and it may never raise or it would become a new failure of exactly the kind it exists to expose -- so emit() reports failure by return value and coerces unserializable values with default=str. curate() now RETURNS the record it logs, so log and behaviour cannot disagree, with an `outcome` that separates the cases that used to look identical (applied / dry_run / skipped+interval / error / unparseable / empty) plus per-action skip reasons (pinned / too_young / not_a_learned_skill / unknown_op) and per-action failures. `proposed == kept + applied + skipped` is the reconciliation check that would have caught the original defect on day one. Split the judgement out into plan_actions() so --dry-run runs the REAL decision rather than a parallel approximation free to drift; a preview deliberately ignores the interval guard (ask any time) and never stamps the clock (must not consume the day's real run). learn/usage log only what is worth reading -- the reentrancy sentinel and ordinary Skill calls fire constantly and would bury the signal -- but learn's `no_transcript_path` (with the hook_keys it DID receive) and usage's `unreadable_skill_name` are now the loop's only detector for Claude Code renaming a SessionEnd/PreToolUse payload key; both paths previously just returned 0. 82 pytest (was 73 after this branch's curator batch, 55 at branch point), full BCT gate green (XCTest 902/0, Swift Testing 3217 in 403 suites, ** TEST SUCCEEDED **, rc=0), all six scripts py_compile + import clean under system python 3.9.
- 2026-07-30 | [curator-observ] LIVE-VERIFIED the whole chain end to end, and the live run immediately paid for itself by exposing a defect the 82-test suite could not: the run log ALREADY held ten events nobody had run -- four curator records dated 2026-07-15 (the tests' frozen NOW, manifest_count 1) and six learn records 300ms apart. Cause: several tests call main() without redirecting log_target, so `uv run pytest` forged events into the user's real ~/.claude/skill-loop.jsonl -- the exact artifact you consult to learn what the loop really did. A diagnostic log the test suite can forge is worse than no log, so isolation is now autouse in a new tests/conftest.py rather than each test's job to remember; it applies before the test body so a test that redirects log_target itself still wins. The two tests whose SUBJECT is path resolution get the pre-fixture function via an `unpatched_log_path` fixture (captured at conftest import) instead of contorting production code. Added a test that pins the guard itself by asserting all four resolvers land on the isolated file and never on $HOME. Verified by line count: real log 11 lines before a full suite run, 11 after (was +10 per run). Purged the 10 forged events, leaving the single genuine record. 83 pytest, gate re-run green (XCTest 902/0, Swift Testing 3217/403, rc=0).
- 2026-07-30 | [curator-observ] Deployed and ran the first real curation decision in the loop's history. Live payload + /Applications/BCT.app bundle both carry runlog/slug-fix/--dry-run (grep-verified on the deployed files, not assumed); bundle patched via the staged-copy route (ditto to staging -> patch + codesign THERE -> rm -rf + ditto into /Applications, BCT confirmed not running first) and re-verified Authority=Apple Development (K83K59TGLX), flags=0x0(none) i.e. NOT adhoc so the IME stays safe. Bundle patch was mandatory, not cosmetic: SkillLoopDeploy.deploy() overwrites ~/.claude/scripts/skill-loop unconditionally at every launch, so a stale bundle would have silently reverted the fix on the next BCT start. Promoted curator_model claude-opus-4-8 -> claude-opus-5 in ~/.claude/skill-loop.json (learn stays claude-sonnet-5; resolution verified through the live payload). `curator.py --dry-run` against the real 63-skill manifest: outcome=dry_run, 63 proposed, 52 keep, 62.2s on opus-5, would_apply = 5 archives (ad-adminsdholder-write-failure-diagnosis, browser-single-site-403-diagnosis, claude-code-hook-windows-port, claude-code-worktree-fake-move-diagnosis, panos-govpn-vendor-nonstandard-port-rule) + 6 pins (devmode-plugin-deploy-check, devmode-task-worktree-pitfalls, devmode-taskjs-behavior-change-landing, op-cli-auth-prompt-storm-diagnosis, panos-api-vpn-reachability-and-ruleadd, xlsx-cell-patch-preserve-parts). Nothing applied, .curator_state untouched -- the preview is genuinely inert. Note the model is FAR more conservative than the raw numbers suggested: 32 of 63 skills have never been used, yet it proposed archiving only 5, keeping the rest as still-plausible. That judgement was there all along and was being thrown away.
